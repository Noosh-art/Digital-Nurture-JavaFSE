# Lab 07 - shoppingapp (Props)

```
npx create-react-app shoppingapp
cd shoppingapp
# copy src/App.js
npm start
```
`OnlineShopping` holds an array of 5 `Cart` items and renders each one
by passing `itemname` and `price` as props to the `Cart` class component.
