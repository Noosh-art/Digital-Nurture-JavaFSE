# Lab 13 - bloggerapp (Conditional Rendering Variations)

```
npx create-react-app bloggerapp
cd bloggerapp
# copy src/BookDetails.js, src/BlogDetails.js, src/CourseDetails.js, src/App.js
npm start
```
- `BookDetails` uses an if/else helper function returning JSX
- `BlogDetails` uses the ternary operator
- `CourseDetails` uses the `&&` short-circuit / element-variable style
